#ifndef IO_PRINT_UTILS_H_
#define IO_PRINT_UTILS_H_

#include "../data_structures/linked_list.h"

/**
 * Prints the details of a node.
 *
 * @param node A pointer to the node to be printed.
 */
void print_node(node_t *node);

/**
 * Prints the details of a linked list.
 *
 * @param list A pointer to the linked list to be printed.
 */
void print_list(linked_list_t *list);

#endif // IO_PRINT_UTILS_H_
